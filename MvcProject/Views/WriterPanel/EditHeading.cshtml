@model EntityLayer.Concrete.Heading
@{
    ViewBag.Title = "EditHeading";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}

<h2>Başlık Güncelleme Sayfası</h2>
@using (Html.BeginForm("EditHeading", "WriterPanel", FormMethod.Post))
{
    <div class="form-group">
        @*@Html.Label("Başlık ID")
            @Html.TextBoxFor(x => x.HeadingID, new { @class = "form-control" })*@
        @Html.HiddenFor(x => x.HeadingID) @* Ekranda gözükmesini istemediğimiz HiddenFor ile gizleriz. *@
        @Html.HiddenFor(x=>x.HeadingDate)
        @Html.HiddenFor(x=>x.WriterID)
        <br />
        @Html.Label("Başlık Adı")
        @Html.TextBoxFor(x => x.HeadingName, new { @class = "form-control" })
        <br />
        @Html.Label("Kategori")
        @Html.DropDownListFor(x => x.CategoryID, (List<SelectListItem>)ViewBag.vlc, new { @class = "form-control" })
        <br />
        <button class="btn btn-success">Güncelle</button>
    </div>
}

